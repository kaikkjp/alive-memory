# Runtime dependencies for the Alive shopkeeper system
#
# Required — system will not start without these
aiosqlite>=0.20.0,<1.0.0
anthropic>=0.49.0,<1.0.0  # fallback only — remove after migration proven
colorama>=0.4.6,<1.0.0  # terminal + server color output (module-level import)
httpx>=0.27.0,<1.0.0    # direct HTTP client for OpenRouter (llm/client.py)
python-dotenv>=1.0.0,<2.0.0

# Optional — features degrade gracefully if missing
aiohttp>=3.9.0,<4.0.0     # ambient weather via wttr.in
tweepy>=4.14.0,<5.0.0     # X/Twitter API client (TASK-057)
certifi>=2024.0.0          # SSL CA bundle for fal.ai CDN downloads
fal-client>=0.5.0          # image generation (fal.ai Seedream)
feedparser>=6.0.0,<7.0.0   # RSS feed ingestion
pyyaml>=6.0                # prompt config (config/prompts.yaml)
sqlite-vec>=0.1.6,<1.0.0   # cold memory vector search (Phase 2)
Pillow>=10.0.0              # OG image compositing
websockets>=12.0            # window viewer websocket server

# Analysis / experiments
matplotlib>=3.8.0             # entropy figures (experiments/)
numpy>=1.26.0                 # numerical analysis (experiments/)

# Testing
pytest>=8.0
pytest-asyncio>=0.24
